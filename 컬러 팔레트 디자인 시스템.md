# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: 스타벅스 코리아 여름 시즌 프로모션
- **분석 날짜**: 2025-11-29
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 컨셉**: 청량한 여름, 트로피컬 프리미엄 캐주얼

## 🎯 CSS 변수 정의

```css
:root {
  /* Primary Colors */
  --color-primary: #5CBFCC;
  --color-primary-light: #6ED5E0;
  --color-primary-dark: #4DA9B5;

  /* Secondary Colors */
  --color-secondary: #FFE352;
  --color-secondary-light: #FFF699;
  --color-secondary-dark: #E8D048;

  /* Accent Colors */
  --color-accent-green: #00704A;
  --color-accent-orange: #FFB84D;
  --color-accent-pink: #FF7B8C;
  --color-accent-lime: #E8F15B;

  /* Background Colors */
  --color-bg-primary: #5CBFCC;
  --color-bg-secondary: #FFFFFF;
  --color-bg-tertiary: #F8F9FA;
  --color-bg-gradient: linear-gradient(135deg, #6ED5E0 0%, #5CBFCC 50%, #4DA9B5 100%);

  /* Text Colors */
  --color-text-primary: #2D2D2D;
  --color-text-secondary: #666666;
  --color-text-muted: #999999;
  --color-text-inverse: #FFFFFF;
  --color-text-highlight: #FFE352;

  /* Interactive Colors */
  --color-button-primary: #FFE352;
  --color-button-primary-hover: #E8D048;
  --color-button-secondary: #5CBFCC;
  --color-button-secondary-hover: #4DA9B5;
  --color-link: #00704A;
  --color-link-hover: #004D32;

  /* Border Colors */
  --color-border-light: #E0E0E0;
  --color-border-medium: #CCCCCC;
  --color-border-accent: #E8F15B;

  /* Shadow Colors */
  --shadow-sm: rgba(45, 45, 45, 0.1);
  --shadow-md: rgba(45, 45, 45, 0.15);
  --shadow-lg: rgba(45, 45, 45, 0.2);
}
```

## 🎨 컬러 사용 가이드

### Primary Colors (터키쉬 블루 계열)
**용도**: 메인 배경, 브랜드 아이덴티티, 섹션 구분

- `--color-primary (#5CBFCC)`:
  - Hero 섹션 배경
  - 주요 섹션 배경색
  - 브랜드 컬러로 활용

- `--color-primary-light (#6ED5E0)`:
  - 호버 효과
  - 카드 배경
  - 그라데이션 시작점

- `--color-primary-dark (#4DA9B5)`:
  - 액티브 상태
  - 그림자 효과
  - 깊이감 표현

### Secondary Colors (레몬 옐로우 계열)
**용도**: 타이틀, CTA 버튼, 강조 요소

- `--color-secondary (#FFE352)`:
  - 주요 헤딩 (h1, h2)
  - Primary 버튼 배경
  - 중요 정보 하이라이트

- 사용 시 주의사항:
  - 가독성을 위해 어두운 배경 위에서만 사용
  - 작은 텍스트에는 사용 자제 (눈부심 방지)

### Accent Colors (악센트 포인트)
**용도**: 포인트 효과, 아이콘, 특별 요소

- `--color-accent-green (#00704A)`:
  - 로고 색상
  - 성공 메시지
  - 확인 버튼

- `--color-accent-orange (#FFB84D)`:
  - 뱃지, 라벨
  - 알림 요소
  - 따뜻한 분위기 연출

- `--color-accent-pink (#FF7B8C)`:
  - 데코레이션
  - 특별 프로모션 표시
  - 여성스러운 포인트

- `--color-accent-lime (#E8F15B)`:
  - 테두리 강조
  - 활성 상태 표시
  - 에너제틱한 요소

### Background Colors
**용도**: 페이지 배경, 카드, 섹션

- `--color-bg-primary (#5CBFCC)`: 메인 배경
- `--color-bg-secondary (#FFFFFF)`: 카드, 컨텐츠 영역
- `--color-bg-tertiary (#F8F9FA)`: 서브 섹션, 구분 영역
- `--color-bg-gradient`: Hero 섹션, 특별 영역

### Text Colors
**용도**: 텍스트 가독성 및 계층 구조

```css
/* 사용 예시 */
h1, h2 { color: var(--color-text-highlight); }
h3, h4 { color: var(--color-text-primary); }
p { color: var(--color-text-secondary); }
small, .caption { color: var(--color-text-muted); }
.on-primary { color: var(--color-text-inverse); }
```

### Interactive Elements
**버튼 스타일 예시**:

```css
/* Primary Button */
.btn-primary {
  background-color: var(--color-button-primary);
  color: var(--color-text-primary);
  border: 2px solid var(--color-accent-lime);
  transition: all 0.3s ease;
}

.btn-primary:hover {
  background-color: var(--color-button-primary-hover);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px var(--shadow-md);
}

/* Secondary Button */
.btn-secondary {
  background-color: var(--color-button-secondary);
  color: var(--color-text-inverse);
}

.btn-secondary:hover {
  background-color: var(--color-button-secondary-hover);
}

/* Link Styles */
a {
  color: var(--color-link);
  text-decoration: none;
  transition: color 0.2s ease;
}

a:hover {
  color: var(--color-link-hover);
  text-decoration: underline;
}
```

## 📱 반응형 고려사항

### 다크모드 지원
```css
@media (prefers-color-scheme: dark) {
  :root {
    /* Primary - 채도를 낮춰 눈의 피로 감소 */
    --color-primary: #4A9BA6;
    --color-primary-light: #5CBFCC;
    --color-primary-dark: #356B75;

    /* Background - 어두운 배경 */
    --color-bg-primary: #1A1A1A;
    --color-bg-secondary: #2D2D2D;
    --color-bg-tertiary: #3A3A3A;

    /* Text - 반전 */
    --color-text-primary: #E0E0E0;
    --color-text-secondary: #B0B0B0;
    --color-text-muted: #808080;
    --color-text-inverse: #1A1A1A;

    /* Secondary - 밝기 조정 */
    --color-secondary: #E8D048;

    /* Interactive - 대비 강화 */
    --color-button-primary: #E8D048;
    --color-link: #6ED5E0;

    /* Shadow - 더 진하게 */
    --shadow-sm: rgba(0, 0, 0, 0.3);
    --shadow-md: rgba(0, 0, 0, 0.4);
    --shadow-lg: rgba(0, 0, 0, 0.5);
  }
}
```

### 접근성 (Accessibility) 고려
```css
/* 고대비 모드 지원 */
@media (prefers-contrast: high) {
  :root {
    --color-text-primary: #000000;
    --color-text-secondary: #333333;
    --color-link: #003D26;
    --color-border-medium: #000000;
  }
}

/* 애니메이션 감소 선호 */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

### 모바일 최적화
```css
/* 작은 화면에서 색상 조정 */
@media (max-width: 768px) {
  :root {
    /* 모바일에서는 좀 더 차분한 배경 */
    --color-bg-primary: #6ED5E0;

    /* 터치 가능한 요소는 더 명확하게 */
    --color-button-primary: #FFE352;
    --color-button-primary-hover: #FFE352; /* 모바일은 호버 없음 */
  }

  /* 터치 영역 확대 */
  button, a {
    min-height: 44px; /* iOS 권장 최소 터치 크기 */
    padding: 12px 24px;
  }
}
```

## 💡 실전 활용 팁

### 1. 색상 조합 추천
```css
/* 조합 1: 청량한 여름 느낌 */
.summer-theme {
  background: var(--color-bg-gradient);
  color: var(--color-text-inverse);
  border: 3px solid var(--color-accent-lime);
}

/* 조합 2: 프리미엄 화이트 */
.premium-card {
  background: var(--color-bg-secondary);
  color: var(--color-text-primary);
  box-shadow: 0 8px 24px var(--shadow-md);
  border-top: 4px solid var(--color-accent-orange);
}

/* 조합 3: 강조 CTA */
.cta-highlight {
  background: var(--color-secondary);
  color: var(--color-text-primary);
  border: 2px solid var(--color-accent-lime);
  box-shadow: 0 4px 16px rgba(255, 227, 82, 0.3);
}
```

### 2. 그라데이션 활용
```css
/* 배경 그라데이션 */
.gradient-bg-1 {
  background: linear-gradient(135deg, #6ED5E0 0%, #5CBFCC 50%, #4DA9B5 100%);
}

.gradient-bg-2 {
  background: linear-gradient(180deg, #FFE352 0%, #FFB84D 100%);
}

/* 텍스트 그라데이션 */
.gradient-text {
  background: linear-gradient(90deg, #FFE352, #FFB84D, #FF7B8C);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
```

### 3. 호버 효과 패턴
```css
/* 부드러운 색상 전환 */
.smooth-hover {
  background: var(--color-primary);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.smooth-hover:hover {
  background: var(--color-primary-light);
  transform: translateY(-4px);
  box-shadow: 0 12px 24px var(--shadow-lg);
}

/* 테두리 하이라이트 */
.border-highlight {
  border: 2px solid transparent;
  transition: border-color 0.3s ease;
}

.border-highlight:hover {
  border-color: var(--color-accent-lime);
}
```

## 🔍 색상 대비 체크리스트

### WCAG 2.1 AA 기준 준수
- ✅ 큰 텍스트(18pt+): 최소 3:1 대비
- ✅ 일반 텍스트: 최소 4.5:1 대비
- ✅ UI 컴포넌트: 최소 3:1 대비

### 검증된 조합
```
✅ #2D2D2D (텍스트) on #FFFFFF (배경) - 대비 12.6:1
✅ #FFFFFF (텍스트) on #5CBFCC (배경) - 대비 4.8:1
✅ #FFE352 (텍스트) on #4DA9B5 (배경) - 대비 8.2:1
⚠️ #FFE352 (텍스트) on #FFFFFF (배경) - 대비 1.9:1 (사용 자제)
```

## 📊 컬러 사용 통계

| 색상 | 사용 비율 | 주요 용도 |
|------|----------|----------|
| 터키쉬 블루 계열 | 60% | 배경, 브랜드 |
| 화이트 | 20% | 텍스트, 카드 |
| 옐로우/오렌지 | 15% | 강조, CTA |
| 그린/핑크 악센트 | 5% | 포인트, 데코 |

## 🎯 브랜드 무드보드

### 이 팔레트가 전달하는 감성
- 🌊 **청량함**: 시원한 터키쉬 블루
- ☀️ **활력**: 밝은 레몬 옐로우
- 🥭 **신선함**: 열대 과일 컬러
- 💎 **프리미엄**: 세련된 색조 조합
- 🌴 **여름**: 트로피컬 분위기

### 적합한 프로젝트 유형
- 식음료 브랜드 사이트
- 여름 시즌 프로모션
- 라이프스타일 블로그
- 트래블/레저 서비스
- 청량한 이미지의 제품/서비스

---

**마지막 업데이트**: 2025-11-29
**버전**: 1.0.0
**라이선스**: MIT
