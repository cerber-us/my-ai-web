# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: 혁신아이콘 모집 포스터
- **분석 날짜**: 2025-12-14
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 컨셉**: 모던하고 역동적인 3D 기하학 디자인

---

## 🎯 CSS 변수 정의

```css
:root {
  /* Primary Colors - Cyan/Blue */
  --color-primary: #00BCD4;
  --color-primary-light: #00E5FF;
  --color-primary-dark: #0097A7;

  /* Secondary Colors - Gold/Yellow */
  --color-secondary: #FFD700;
  --color-secondary-light: #FFEB3B;
  --color-secondary-dark: #FFA000;

  /* Accent Colors */
  --color-accent-pink: #FF69B4;
  --color-accent-pink-dark: #FF1493;
  --color-accent-purple: #9370DB;
  --color-accent-purple-dark: #BA55D3;
  --color-accent-orange: #FFA500;
  --color-accent-mint: #40E0D0;

  /* Background Colors */
  --color-bg-primary: #FFF9E6;
  --color-bg-secondary: #FFFFFF;
  --color-bg-gradient: linear-gradient(135deg, #FFF9E6 0%, #FFFFFF 100%);

  /* Text Colors */
  --color-text-primary: #000000;
  --color-text-secondary: #333333;
  --color-text-muted: #666666;
  --color-text-light: #999999;

  /* Border & Outline */
  --color-border-light: #E0E0E0;
  --color-border-medium: #CCCCCC;
  --color-shadow: rgba(0, 0, 0, 0.1);

  /* Interactive Colors */
  --color-button-primary: #00BCD4;
  --color-button-hover: #00E5FF;
  --color-button-active: #0097A7;
  --color-link: #00BCD4;
  --color-link-hover: #FFD700;
  --color-link-visited: #9370DB;

  /* Overlay & Transparency */
  --overlay-light: rgba(255, 255, 255, 0.3);
  --overlay-medium: rgba(255, 255, 255, 0.5);
  --overlay-dark: rgba(0, 0, 0, 0.5);
}
```

---

## 🎨 컬러 사용 가이드

### 1️⃣ Primary Color (Cyan #00BCD4)
**사용 시나리오:**
- 주요 CTA 버튼 (회원가입, 시작하기 등)
- 네비게이션 활성 상태
- 주요 아이콘 및 그래픽 요소
- 로딩 인디케이터

**사용 예시:**
```css
.primary-button {
  background-color: var(--color-primary);
  color: white;
  transition: all 0.3s ease;
}

.primary-button:hover {
  background-color: var(--color-primary-light);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(0, 188, 212, 0.3);
}
```

### 2️⃣ Secondary Color (Gold #FFD700)
**사용 시나리오:**
- 강조가 필요한 정보 (가격, 특별 혜택)
- 두 번째 CTA 버튼
- 배지, 태그, 라벨
- 중요 알림 메시지

**사용 예시:**
```css
.highlight-badge {
  background-color: var(--color-secondary);
  color: var(--color-text-primary);
  padding: 4px 12px;
  border-radius: 16px;
  font-weight: bold;
}
```

### 3️⃣ Accent Colors (Pink, Purple, Orange)
**사용 시나리오:**
- **Pink (#FF69B4)**: 좋아요, 즐겨찾기, 특별 이벤트
- **Purple (#9370DB)**: 프리미엄 기능, VIP 콘텐츠
- **Orange (#FFA500)**: 알림, 경고, 긴급 메시지

**사용 예시:**
```css
.accent-card {
  border-left: 4px solid var(--color-accent-pink);
  background: linear-gradient(135deg,
    rgba(255, 105, 180, 0.1) 0%,
    transparent 100%);
}

.premium-feature {
  color: var(--color-accent-purple);
  font-weight: 600;
}
```

### 4️⃣ Background Colors
**사용 시나리오:**
- **Primary BG (#FFF9E6)**: 전체 페이지 배경
- **Secondary BG (#FFFFFF)**: 카드, 모달, 섹션 배경

**사용 예시:**
```css
body {
  background-color: var(--color-bg-primary);
}

.card {
  background-color: var(--color-bg-secondary);
  border-radius: 12px;
  box-shadow: 0 2px 8px var(--color-shadow);
}
```

### 5️⃣ Text Colors
**계층 구조:**
- **Primary (#000000)**: 제목, 중요한 텍스트
- **Secondary (#333333)**: 본문 텍스트
- **Muted (#666666)**: 부가 정보, 캡션
- **Light (#999999)**: 비활성 상태, placeholder

**가독성 가이드:**
```css
h1, h2, h3 {
  color: var(--color-text-primary);
  font-weight: bold;
}

p {
  color: var(--color-text-secondary);
  line-height: 1.6;
}

.caption {
  color: var(--color-text-muted);
  font-size: 0.875rem;
}
```

---

## 📱 반응형 고려사항

### 🌙 다크모드 지원
```css
@media (prefers-color-scheme: dark) {
  :root {
    /* Background - 반전 */
    --color-bg-primary: #1a1a1a;
    --color-bg-secondary: #2d2d2d;

    /* Text - 밝게 조정 */
    --color-text-primary: #FFFFFF;
    --color-text-secondary: #E0E0E0;
    --color-text-muted: #A0A0A0;

    /* Primary - 밝기 증가 */
    --color-primary: #00E5FF;
    --color-primary-light: #4DFFFF;

    /* Secondary - 채도 감소 */
    --color-secondary: #FFE066;

    /* Accent - 대비 강화 */
    --color-accent-pink: #FF80C4;
    --color-accent-purple: #B399E6;

    /* Shadow - 어둡게 */
    --color-shadow: rgba(0, 0, 0, 0.5);
  }
}
```

### 📐 모바일 최적화
```css
/* 모바일에서 터치 영역 확보 */
@media (max-width: 768px) {
  .primary-button {
    min-height: 44px;
    padding: 12px 24px;
    font-size: 16px;
  }

  /* 강조 색상을 더 선명하게 */
  :root {
    --color-primary: #00E5FF;
    --color-secondary: #FFD700;
  }
}
```

### 🎨 투명도 활용 (3D 효과)
```css
.glass-effect {
  background: var(--overlay-medium);
  backdrop-filter: blur(10px);
  border: 1px solid var(--overlay-light);
  box-shadow:
    0 8px 32px rgba(0, 0, 0, 0.1),
    inset 0 1px 1px rgba(255, 255, 255, 0.3);
}

.gradient-card {
  background: linear-gradient(
    135deg,
    rgba(0, 188, 212, 0.1) 0%,
    rgba(255, 215, 0, 0.1) 50%,
    rgba(255, 105, 180, 0.1) 100%
  );
  border-radius: 16px;
}
```

### ♿ 접근성 (WCAG 2.1 준수)
```css
/* 대비율 4.5:1 이상 유지 */
.accessible-button {
  background-color: var(--color-primary);
  color: #FFFFFF; /* 대비율: 7.5:1 */
}

/* 포커스 상태 명확하게 */
button:focus-visible {
  outline: 3px solid var(--color-secondary);
  outline-offset: 2px;
}

/* 링크 밑줄 표시 (색각 이상 대응) */
a {
  color: var(--color-link);
  text-decoration: underline;
  text-decoration-thickness: 2px;
  text-underline-offset: 3px;
}
```

---

## 🔧 실전 활용 예시

### Hero Section
```css
.hero {
  background: var(--color-bg-gradient);
  padding: 80px 20px;
  position: relative;
  overflow: hidden;
}

.hero::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -20%;
  width: 500px;
  height: 500px;
  background: radial-gradient(
    circle,
    rgba(0, 188, 212, 0.2) 0%,
    transparent 70%
  );
}

.hero-title {
  color: var(--color-text-primary);
  font-size: 3rem;
  font-weight: bold;
  margin-bottom: 1rem;
}

.hero-cta {
  background-color: var(--color-primary);
  color: white;
  padding: 16px 32px;
  border-radius: 8px;
  transition: all 0.3s ease;
}

.hero-cta:hover {
  background-color: var(--color-primary-light);
  transform: scale(1.05);
  box-shadow: 0 8px 24px rgba(0, 188, 212, 0.4);
}
```

### Card Component
```css
.feature-card {
  background: var(--color-bg-secondary);
  border-radius: 16px;
  padding: 24px;
  box-shadow: 0 4px 16px var(--color-shadow);
  border-left: 4px solid var(--color-primary);
  transition: transform 0.3s ease;
}

.feature-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 12px 32px rgba(0, 188, 212, 0.2);
}

.feature-icon {
  width: 64px;
  height: 64px;
  background: linear-gradient(135deg,
    var(--color-primary) 0%,
    var(--color-secondary) 100%);
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16px;
}
```

---

## 📚 참고 자료

### 컬러 조합 비율
- **Primary (Cyan)**: 30% - 브랜드 아이덴티티
- **Secondary (Gold)**: 20% - 강조 요소
- **Accent (Pink/Purple)**: 20% - 시각적 다양성
- **Background (Cream/White)**: 25% - 여백과 가독성
- **기타 (Orange/Mint)**: 5% - 포인트 컬러

### 브랜드 키워드
- ✨ **혁신적** (Innovative)
- 🌈 **역동적** (Dynamic)
- 💫 **창의적** (Creative)
- 🎯 **젊은** (Youthful)
- 🚀 **미래지향적** (Forward-thinking)

### 추천 활용 분야
- 스타트업 랜딩 페이지
- 포트폴리오 웹사이트
- 이벤트/프로모션 페이지
- 교육 플랫폼
- 크리에이티브 에이전시

---

**마지막 업데이트**: 2025-12-14
**버전**: 1.0
**작성자**: AI 컬러 분석 시스템
